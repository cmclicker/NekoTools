# NekoSecrets demo input — 100% SYNTHETIC. None of these are real credentials.
# Load this file in NekoSecrets (Load a local file) or paste it. Nothing is uploaded.

# --- high severity (provider patterns) ---
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
GITHUB_TOKEN=ghp_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
OPENAI_API_KEY=sk-proj-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
DATABASE_URL=postgres://app:s3cr3tpassword@db.internal:5432/app

# --- medium severity ---
# (provider-prefixed keys like Stripe/Slack/Twilio are detected too; they're
#  omitted from this committed fixture so it doesn't trip secret scanners.)
DB_PASSWORD = "hunter2hunter2"

# --- low severity (entropy fallback: high-randomness token, no known prefix) ---
SESSION_BLOB=Zk7Q9pX2vL4mN8rT1yB6cF3hJ0dK5sW7aE2gU9iO

# --- not flagged (ordinary config) ---
LOG_LEVEL=info
FEATURE_FLAG_NEW_UI=true
note: this line is fine and will not be flagged
